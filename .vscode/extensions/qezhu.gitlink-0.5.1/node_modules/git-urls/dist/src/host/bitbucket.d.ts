import BasicHost from "./basicHost";
import GitInfo from "../gitInfo";
export default class BitBucket extends BasicHost {
    protected separateFolder: string;
    assemble(info: GitInfo): string;
}
