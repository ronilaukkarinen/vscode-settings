import ConfigInfo from "./configInfo";
export default class Helper {
    static parseConfigAsync(repoRoot: string): Promise<Map<string, ConfigInfo>>;
    static getRepoRoot(filePath: string): string | null;
    static normarlize(filePath: string): string;
    private static parseRemoteUrl;
    private static parseBranchName;
}
